services:
  sveltedraw:
    build: .
    ports:
      - "5173:5173"
    volumes:
      # Mount source for hot reload
      - ./src:/app/src
      - ./static:/app/static
      # Exclude node_modules from mount
      - /app/node_modules
    environment:
      - NODE_ENV=development
